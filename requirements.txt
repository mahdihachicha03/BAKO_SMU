# Bako CAN Dashboard — Python dependencies
# Install with: pip install -r requirements.txt

fastapi==0.115.0      # Web framework + WebSocket support
uvicorn==0.29.0       # ASGI server to run FastAPI
pyserial==3.5         # Serial port communication with ESP32
